
<style>
  tr {
    line-height: 55px;
    min-height: 25px;
    height: 40px;
    text-align: left;
  }
  .table tbody>tr>td{
    vertical-align:middle;
  }

</style>
<div class="container">

  <div class="row">
    <div id="breadcrumb">
      <ul class="crumbs">
        <li class="first"><a href="/" style="z-index:9;"><span></span>Home</a></li>
        <li><a class="current" href="/courses/byuser/<%= 1 %>" style="z-index:8;">My Courses</a></li>
      </ul>
    </div>
  </div>

  <div class="col-lg-9">

    <!-- Form Name -->
    <legend>My courses</legend>

    <div class="col-md-12">
      <div class="table-responsive">
        <table class="table table-striped table-hover table-condensed">
          <thead>
          <tr>
            <th>Course Name</th>
            <th>Level</th>
            <th>Duration</th>
            <th>Sections</th>
            <th>Status</th>
            <th>View times</th>
            <th>Rank</th>
          </tr>
          </thead>
          <tbody>

    <% courses.forEach( function(course) { %>
    <%
      var sections = course.getSections();
      sections = sections;
    %>

    <tr>
      <td> <a href="/courses/byid/<%= course.enId() %>"> <%= course.name %> </a></td>
      <td> <%= course.level%></td>
      <td><%= parseInt(course.duration / 60) %> m <%= course.duration % 60 %> s</td>
      <td><%= course.sections.length%></td>
      <td><%= course.status %></td>
      <td><%= course.viewtimes %></td>
      <td><%= course.rank %></td>
    </tr>

    <!--
    <div class="columns large-12">
      <div class="row">
        <div class="columns">
          <strong>
            <a href="/courses/byid/<%= course.enId() %>"> <%= course.name %> </a>
          </strong>

          <div class="well well-lg">
            <div class="row">
              <div class="col-md-3 text-right">
                <strong>Course Description: </strong>
              </div>
              <div class="col-md-6">
                <%= course.desc %>
              </div>
            </div>

            <div class="row">
              <div class="col-md-3 text-right">
                <strong>Course tags: </strong>
              </div>
              <div class="col-md-6">
                <%= course.tags %>
              </div>
            </div>

            <div class="row">
              <div class="col-md-3 text-right">
                <strong>Duration: </strong>
              </div>
              <div class="col-md-6">
                <%= parseInt(course.duration / 60) %> Minutes <%= course.duration % 60 %> seconds
              </div>
            </div>

            <div class="row">
              <div class="col-md-3 text-right">
                <strong>Level: </strong>
              </div>
              <div class="col-md-6">
                <%= course.level %>
              </div>
            </div>

            <div class="row">
              <div class="col-md-3 text-right">
                <strong>Status: </strong>
              </div>
              <div class="col-md-6">
                <%= course.status %>
              </div>
            </div>

            <div class="row">
              <div class="col-md-3 text-right">
                <strong>Rank: </strong>
              </div>
              <div class="col-md-6">
                <%= course.rank %>
              </div>
            </div>

            <div class="row">
              <div class="col-md-3 text-right">
                <strong>View Times: </strong>
              </div>
              <div class="col-md-6">
                <%= course.viewtimes %>
              </div>
            </div>

            <div class="row">
              <div class="col-md-3 text-right">
                <strong>Course Type: </strong>
              </div>
              <div class="col-md-6">
                <%= course.coursetype %>
              </div>
            </div>

            <div class="row">

              <div class="col-md-3 text-right">
                <strong>Sections: </strong>
              </div>
              <div class="col-md-9">
                <div class="well">
                  <div class="row">
                    <div class="col-md-4 text-right">
                      <strong>Section Number: </strong>
                    </div>
                    <div class="col-md-8 info">
                      <%= course.sections.length %>
                    </div>
                  </div>

                  <%
                    var sections = course.sections;
                    if(sections != null){
                      sections.forEach(function(section){
                  %>

                  <div class="row">
                    <div class="col-md-4 text-right">
                      <strong>Section Name: </strong>
                    </div>
                    <div class="col-md-8">
                      <a href="/courses/<%= course.enId()%>/sections/<%= section.enId() %>">
                      <%= section.title %>
                      </a>
                    </div>
                  </div>
                  <%
                    });
                    }
                  %>

                </div>
              </div>
            </div>
          </div>
       </div>
      </div>
    </div>
  -->
 <% }); %>
          </tbody>
        </table>
      </div>
    </div>

</div>
</div>
